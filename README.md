# Riemannian-Embedded-Transformer
A parameter-efficient Transformer architecture utilizing the tangent bundle of a Riemannian manifold to replace standard Euclidean embeddings, leveraging manifold geometry to reduce model redundancy and parameter count.
